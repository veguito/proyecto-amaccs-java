package proyectospring.gestionusuario.service;

import java.util.Collection;
import java.util.List;
import java.util.Optional;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;


import proyectospring.gestionusuario.model.Auto;



public class AutoService {
	

	/*public List<Auto> filtrarPorEdad(List<Auto> autos, int idFiltro) {
	    
        return autos.stream()
                .filter(auto -> auto.getId() == idFiltro)
                .toList(); 
    }
	
	public int contarMayoresOIguales(List<Auto> autos, int idFiltro) {
	       
        return (int) autos.stream()
                .filter(auto -> auto.getId() == idFiltro)
                .count();
    } 
	
	 @GetMapping
	    public int contarPorid(@RequestParam int id, Collection<Auto> autos) {
			return (int) autos.stream()
	                .filter(auto -> auto.getId() == id)
	                .count();
	   } */
	 
	
	 
	 

}
